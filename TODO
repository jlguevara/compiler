storeai index needs to be fixed i.e. a.b.c = 7
fix conditionals and loops to take just true or false
check that while loops only accept bool expressions
fix needless branches to exit block (might have to use a flag to check that we are on the last return statement of a function)

questions

source and target for adq? addq? sarq?

where do we check if inner structs were initialized?
struct node {
    int x;
    struct node next;
};


multiplying big numbers


why so many loads i.e. z = x + y requires that we load x and y into new registers.  Why not just do add rx, ry, rz?

how to handle conditionals with only "true" or "false" in them

how to handle jumps to exit block (particularly when there is only one return statement.  An example is the bench_factorize program.
